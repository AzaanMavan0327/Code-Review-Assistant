# Core dependencies for the code review assistant.
# Version ranges use >= so minor updates work, but we pin major versions
# to avoid surprise breaking changes.

# Phase 1: CLI framework. Click is cleaner than argparse for multi-command tools.
click>=8.1.0,<9.0.0

# Phase 2: GitHub API client and diff parsing
PyGithub>=2.1.0,<3.0.0
unidiff>=0.7.5,<0.8.0

# Phase 3: LLM client and response caching
anthropic>=0.40.0,<1.0.0
diskcache>=5.6.0,<6.0.0

# Shared: loads variables from .env into os.environ
python-dotenv>=1.0.0,<2.0.0