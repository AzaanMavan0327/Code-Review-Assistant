# Dev/test dependencies. Install with: pip install -r requirements-dev.txt
# This includes everything in requirements.txt, plus testing tools.

-r requirements.txt

# Test framework
pytest>=8.0.0,<9.0.0
pytest-mock>=3.12.0,<4.0.0
pytest-cov>=4.1.0,<5.0.0

# Linter and formatter (optional but professional)
ruff>=0.5.0,<1.0.0